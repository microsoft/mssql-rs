#################################################################################
#                OneBranch Pipeline - mssql-python-rs PyPI Release               #
#                                                                               #
# The single ESRP -> PyPI publish pipeline for the `mssql-python-rs` package.    #
#                                                                               #
# PHASE 1 (now): publishes a tiny placeholder sdist (see .pipeline/pypi-stub/,   #
# version 0.0.0) to CREATE and RESERVE the PyPI project name and prove the ESRP  #
# -> PyPI path end to end.                                                       #
#                                                                               #
# PHASE 2 (later): the build stage below is replaced to collect the real,       #
# signed wheels from the Official Python Wheels Build and publish those. The     #
# ESRP release stage stays as-is. The placeholder 0.0.0 version is yanked once   #
# the first real version ships.                                                 #
#                                                                               #
# Manual trigger only. `publishToPyPI` defaults to false: a default run is a    #
# dry run (build + `twine check`) with NO publish. Flip it to true only for     #
# a deliberate publish.                                                          #
#                                                                               #
# Reuses the shared 'ESRP Federated Creds (AME)' variable group (same AKV cert  #
# / ESRP creds mssql-python publishes with).                                    #
#################################################################################

name: $(Year:YY)$(DayOfYear)$(Rev:.r)-PyPI-Release

trigger: none
pr: none

parameters:
  - name: publishToPyPI
    displayName: 'Publish stub to PyPI via ESRP (real reservation). Leave false for a dry run.'
    type: boolean
    default: false

variables:
  - name: CDP_DEFINITION_BUILD_COUNT
    value: $[counter('', 0)]
  - name: LinuxContainerImage
    value: 'mcr.microsoft.com/onebranch/azurelinux/build:3.0'
  - name: WindowsContainerImage
    value: 'onebranch.azurecr.io/windows/ltsc2022/vse2022:latest'
  - name: DEBIAN_FRONTEND
    value: noninteractive
  - group: 'ESRP Federated Creds (AME)'

resources:
  repositories:
    - repository: templates
      type: git
      name: OneBranch.Pipelines/GovernedTemplates
      ref: refs/heads/main

# Official template: required for a real production publish to public PyPI. The
# pipeline must be classified Production in the product catalog for this to run.
extends:
  template: v2/OneBranch.Official.CrossPlat.yml@templates
  parameters:
    featureFlags:
      EnableCDPxPAT: false
      WindowsHostVersion: 1ESWindows2022
    globalSdl:
      binskim:
        enabled: true
        scanOutputDirectoryOnly: true
      credscan:
        enabled: true
      policheck:
        enabled: true

    stages:
    #########################################################################
    # One stage, two jobs. The stub sdist is prebuilt and committed under
    # .pipeline/pypi-stub/dist/, so the pipeline builds nothing: the sealed
    # OneBranch container has no Python and no outbound network. OneBranch
    # auto-uploads whatever lands in ob_outputDirectory (no publish task).
    #########################################################################
    - stage: ReleaseStub
      displayName: 'Publish mssql-python-rs stub'
      jobs:
      #####################################################################
      # Job 1 (always): verify the committed stub sdist. On a default (dry)
      # run this is the only job, so a green run proves the pipeline compiles
      # and the ESRP variable group authorizes, without publishing anything.
      #####################################################################
      - job: VerifyStub
        displayName: 'Verify committed stub sdist'
        pool:
          type: windows
        variables:
          ob_outputDirectory: '$(Build.ArtifactStagingDirectory)'
        steps:
        - pwsh: |
            $ErrorActionPreference = 'Stop'
            $distDir = "$(Build.SourcesDirectory)/.pipeline/pypi-stub/dist"
            $sdists = @(Get-ChildItem "$distDir" -Filter 'mssql_python_rs-*.tar.gz' -ErrorAction SilentlyContinue)
            if ($sdists.Count -ne 1) {
              Write-Error "Expected exactly one committed stub sdist under $distDir, found $($sdists.Count)."
              exit 1
            }
            $sdists | Format-Table Name, Length
            # Surface the sdist as a pipeline artifact via ob_outputDirectory.
            Copy-Item $sdists[0].FullName -Destination "$(Build.ArtifactStagingDirectory)/" -Force
          displayName: 'Verify committed stub sdist'

      #####################################################################
      # Job 2 (gated): publish the committed stub sdist to PyPI via ESRP. The
      # repo is checked out, so ESRP reads the sdist straight from source — no
      # Python, pip, build, or download needed on the sealed pool. Runs ONLY
      # when publishToPyPI is true.
      #####################################################################
      - ${{ if eq(parameters.publishToPyPI, true) }}:
        - job: PublishToPyPI
          displayName: 'ESRP release to PyPI'
          dependsOn: VerifyStub
          templateContext:
            type: releaseJob
            isProduction: true
          pool:
            type: windows
          variables:
            ob_outputDirectory: '$(Build.ArtifactStagingDirectory)'
            WindowsContainerImage: 'onebranch.azurecr.io/windows/ltsc2022/vse2022:latest'
          steps:
          # ContentType PyPI: the real publish. The first PyPI publish of
          # mssql-python-rs creates and reserves the project name.
          - task: EsrpRelease@9
            displayName: 'ESRP Release to PyPI (reserve mssql-python-rs)'
            inputs:
              connectedservicename: '$(ESRPConnectedServiceName)'
              usemanagedidentity: true
              keyvaultname: '$(AuthAKVName)'
              signcertname: '$(AuthSignCertName)'
              clientid: '$(EsrpClientId)'
              Intent: 'PackageDistribution'
              ContentType: 'PyPI'
              ContentSource: 'Folder'
              FolderLocation: '$(Build.SourcesDirectory)/.pipeline/pypi-stub/dist'
              WaitForReleaseCompletion: true
              Owners: '$(owner)'
              Approvers: '$(approver)'
              ServiceEndpointUrl: 'https://api.esrp.microsoft.com'
              MainPublisher: 'ESRPRELPACMAN'
              DomainTenantId: '$(DomainTenantId)'

          - pwsh: |
              Write-Host 'Reservation publish complete: mssql-python-rs 0.0.0 submitted to PyPI via ESRP.'
              Write-Host 'The PyPI project name is now reserved. Yank the stub version once the real release lands.'
            displayName: 'Reservation summary'
