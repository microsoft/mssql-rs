# Performance baseline commit for mssql-odbc.
#
# This pin is the current main commit used as the production base for the
# optimization diff. It builds the benchmark-era driver plus later main changes
# without this PR's optimizations. Future advances require a pull request, so
# every baseline move is reviewed and recorded.
#
# Exactly one full 40-character commit SHA on its own line. Lines starting with
# '#' and blank lines are ignored.
db7d266506c4fbc92b681221d50e686c2926201a
